```
NAME
    reload -- clear cache generated by any command.

USAGE
    pass the name of command you want to reload after !reload.
    
    >>> !reload announce

    reload command can also reload more than one command at
    a time, to do this, pass the name of commands separated
    with spaces.

    >>> !reload announce help hello poll
```